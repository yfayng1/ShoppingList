<%= simple_form_for(@todo) do |f| %>

    <li>
      <%= f.input :done , :label => false %>
      <%= @item.name %>
      <%= link_to 'X', @todo, :confirm => 'Are you sure?', :method => :delete %>
      <%= f.button :submit , :id => 'item_' + @todo.id.to_s %>
    </li>
<% end %>